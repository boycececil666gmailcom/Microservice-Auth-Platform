# Code Organization Guidelines

* **Code Section Separation**:
  Always use `#region <Section Name>` and `#endregion` block comments to logically group and separate modules, endpoints, schemas, configurations, and helper functions within source code files.
